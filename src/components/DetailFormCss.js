import { makeStyles } from "@mui/styles";

export const useStyles= makeStyles({
    heading:{
        display:"flex",
        fontFamily:"Poppins",
        fontSize:"18px",
        fontWeight:"bold",
        textAlign:"center",
        alignItems:"center",
        justifyContent:"center"
    }
})